"""gatekeeper — a vector database built from scratch, for learning.

Import the pieces from their modules, e.g.::

    from gatekeeper.store import VectorStore
    from gatekeeper.distance import cosine, normalize
"""

__version__ = "0.1.0"
